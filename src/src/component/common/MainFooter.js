import React from "react";
import { Layout } from "antd";
const { Footer } = Layout;

const MainFooter = () => {
  return (
    <Footer
      style={{
        textAlign: "center",
        background: '#a4b0be',
      }}
    >
     <b>Leave Management System 2022</b> 
    </Footer>
  );
};

export default MainFooter;
